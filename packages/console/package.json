{
  "name": "@loopkit/console",
  "version": "0.1.0",
  "private": true,
  "description": "Thin generic HTTP console for a loopkit ledger — board/item/needs-you/health views plus four operator verbs (capture/approve/reject/accept); works with JS off (plain HTML forms), progressively enhanced by a small set of server-served scripts",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "public",
    "README.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean:test": "node -e \"require('node:fs').rmSync('dist-test',{recursive:true,force:true})\"",
    "build:test": "npm run clean:test && tsc --project tsconfig.test.json",
    "test": "npm run build && npm run build:test && node --test dist-test/test/*.test.js"
  },
  "dependencies": {
    "@loopkit/core": "*",
    "@loopkit/ui": "*",
    "@loopkit/opsui": "*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.5.0"
  }
}
