{
  "name": "loopkit",
  "version": "0.1.0",
  "private": true,
  "description": "Event-sourced agent plane framework — workspace root",
  "license": "MIT",
  "workspaces": [
    "packages/core",
    "packages/ui",
    "packages/opsui",
    "packages/console"
  ],
  "scripts": {
    "test:browser": "npm run build --workspace=packages/core && npm run build --workspace=packages/ui && npm run build --workspace=packages/opsui && npm run build:test --workspace=packages/console && playwright test",
    "test:browser:ci": "playwright test",
    "test:browser:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
